diff --git a/Localization/Localizable.xcstrings b/Localization/Localizable.xcstrings
index e20c576..db282cb 100644
--- a/Localization/Localizable.xcstrings
+++ b/Localization/Localizable.xcstrings
@@ -11248,7 +11248,7 @@
         "nl" : {
           "stringUnit" : {
             "state" : "translated",
-            "value" : "Bevestig de patch nog niet aan het lichaam"
+            "value" : "Bevestig de patch nog niet op het lichaam"
           }
         },
         "pl" : {
@@ -11410,7 +11410,7 @@
         "nl" : {
           "stringUnit" : {
             "state" : "translated",
-            "value" : "Voltooid"
+            "value" : "Gereed"
           }
         },
         "pl" : {
@@ -35132,7 +35132,133 @@
           }
         }
       }
+    },
+    "Fill guide" : {
+      "comment" : "Title for Medtrum patch fill guide",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Fill guide"
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Vulhandleiding"
+          }
+        }
+      }
+    },
+    "Reservoir: %.2fU / %.0fU" : {
+      "comment" : "Reservoir fill level during Medtrum patch filling",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Reservoir: %.2fU / %.0fU"
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Reservoir: %.2f E / %.0f E"
+          }
+        }
+      }
+    },
+    "Connecting to patch..." : {
+      "comment" : "Status while connecting to Medtrum patch before filling",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Connecting to patch..."
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Verbinden met patch..."
+          }
+        }
+      }
+    },
+    "Minimum fill reached. You can start priming." : {
+      "comment" : "Status when Medtrum patch has enough insulin to prime",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Minimum fill reached. You can start priming."
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Minimale vulling bereikt. Je kunt starten met primen."
+          }
+        }
+      }
+    },
+    "Fill at least 70U before priming." : {
+      "comment" : "Status when Medtrum patch needs more insulin before priming",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Fill at least 70U before priming."
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Vul minstens 70 E voordat je gaat primen."
+          }
+        }
+      }
+    },
+    "Fill the patch with at least 70U before priming." : {
+      "comment" : "Error shown when reservoir has too little insulin before priming",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Fill the patch with at least 70U before priming."
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Vul de patch met minstens 70 E voordat je gaat primen."
+          }
+        }
+      }
+    },
+    "Retry connection" : {
+      "comment" : "label for retrying Medtrum patch fill connection",
+      "extractionState" : "manual",
+      "localizations" : {
+        "en" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Retry connection"
+          }
+        },
+        "nl" : {
+          "stringUnit" : {
+            "state" : "translated",
+            "value" : "Verbinding opnieuw proberen"
+          }
+        }
+      }
     }
   },
   "version" : "1.0"
-}
\ No newline at end of file
+}
diff --git a/MedtrumKit/PumpManager/MedtrumPumpManager.swift b/MedtrumKit/PumpManager/MedtrumPumpManager.swift
index 20b3935..c46c270 100644
--- a/MedtrumKit/PumpManager/MedtrumPumpManager.swift
+++ b/MedtrumKit/PumpManager/MedtrumPumpManager.swift
@@ -654,6 +654,32 @@ public extension MedtrumPumpManager {
         }
     }
 
+    func preparePatchForFilling(_ completion: @escaping (MedtrumPrimePatchResult) -> Void) {
+        log.info("Prepare patch for filling...")
+
+        if state.pumpSN.isEmpty {
+            log.warning("No pump base known yet...")
+            completion(.failure(error: .noKnownPumpBase))
+            return
+        }
+
+        if state.sessionToken.isEmpty {
+            log.debug("Refreshing session token...")
+            state.sessionToken = Crypto.genSessionToken()
+            notifyStateDidChange()
+        }
+
+        bluetooth.ensureConnected { error in
+            if let error = error {
+                self.log.error("Failed to connect to pump: \(error)")
+                completion(.failure(error: .connectionFailure(reason: error.errorDescription ?? "EMPTY")))
+                return
+            }
+
+            completion(.success)
+        }
+    }
+
     func activatePatch(_ completion: @escaping (MedtrumActivatePatchResult) -> Void) {
         log.info("Activate patch pump...")
 
diff --git a/MedtrumKitUI/ViewModels/Onboarding/PatchPrimingViewModel.swift b/MedtrumKitUI/ViewModels/Onboarding/PatchPrimingViewModel.swift
index a416fec..36124a9 100644
--- a/MedtrumKitUI/ViewModels/Onboarding/PatchPrimingViewModel.swift
+++ b/MedtrumKitUI/ViewModels/Onboarding/PatchPrimingViewModel.swift
@@ -1,17 +1,42 @@
+import Foundation
 import LoopKit
 
 class PatchPrimingViewModel: ObservableObject {
     private let processQueue = DispatchQueue(label: "com.nightscout.medtrumkit.primingView")
 
     @Published var isPriming = false
+    @Published var isPreparingFill = false
     @Published var primeProgress: Double = 0
     @Published var primingError = ""
     @Published var is300u = false
+    @Published var reservoirLevel: Double = 0
+    @Published var reservoirCapacity: Double = 200
 
     private let nextStep: () -> Void
     let previousStep: () -> Void
     private let done: () -> Void
     private let pumpManager: MedtrumPumpManager?
+
+    var reservoirProgress: Double {
+        guard reservoirCapacity > 0 else {
+            return 0
+        }
+
+        return min(max(reservoirLevel / reservoirCapacity, 0), 1)
+    }
+
+    var hasMinimumFill: Bool {
+        reservoirLevel >= 70
+    }
+
+    var canStartPrime: Bool {
+        !isPreparingFill && !isPriming && hasMinimumFill
+    }
+
+    var canRetryFillConnection: Bool {
+        !isPreparingFill && !isPriming && !hasMinimumFill
+    }
+
     init(
         _ pumpManager: MedtrumPumpManager?,
         _ nextStep: @escaping () -> Void,
@@ -28,6 +53,8 @@ class PatchPrimingViewModel: ObservableObject {
         }
 
         is300u = pumpManager.state.pumpName.contains("300U")
+        reservoirCapacity = pumpManager.pumpReservoirCapacity
+        reservoirLevel = pumpManager.state.reservoir
         pumpManager.addStatusObserver(self, queue: processQueue)
     }
 
@@ -35,6 +62,31 @@ class PatchPrimingViewModel: ObservableObject {
         pumpManager?.removeStatusObserver(self)
     }
 
+    func prepareForFilling() {
+        #if targetEnvironment(simulator)
+            reservoirLevel = pumpManager?.pumpReservoirCapacity ?? 200
+        #else
+            guard let pumpManager = self.pumpManager,
+                  !isPreparingFill,
+                  pumpManager.state.pumpState.rawValue < PatchState.priming.rawValue
+            else {
+                return
+            }
+
+            isPreparingFill = true
+            primingError = ""
+            pumpManager.preparePatchForFilling { result in
+                DispatchQueue.main.async {
+                    self.isPreparingFill = false
+
+                    if case let .failure(error) = result {
+                        self.primingError = error.description
+                    }
+                }
+            }
+        #endif
+    }
+
     func startPrime() {
         #if targetEnvironment(simulator)
             pumpManager?.state.sessionToken = Crypto.genSessionToken()
@@ -47,6 +99,14 @@ class PatchPrimingViewModel: ObservableObject {
                 return
             }
 
+            guard hasMinimumFill else {
+                primingError = String(
+                    localized: "Fill the patch with at least 70U before priming.",
+                    comment: "Error shown when reservoir has too little insulin before priming"
+                )
+                return
+            }
+
             isPriming = true
             primingError = ""
             pumpManager.primePatch { result in
@@ -88,6 +148,8 @@ extension PatchPrimingViewModel: PumpManagerStatusObserver {
 
             DispatchQueue.main.async {
                 self.primeProgress = Double(pumpManager.state.primeProgress) / 240
+                self.reservoirLevel = pumpManager.state.reservoir
+                self.reservoirCapacity = pumpManager.pumpReservoirCapacity
 
                 if pumpManager.state.pumpState.rawValue > PatchState.priming.rawValue,
                    pumpManager.state.pumpState.rawValue < PatchState.active.rawValue
diff --git a/MedtrumKitUI/Views/Onboarding/PatchPrimingView.swift b/MedtrumKitUI/Views/Onboarding/PatchPrimingView.swift
index b435d4a..2623fbd 100644
--- a/MedtrumKitUI/Views/Onboarding/PatchPrimingView.swift
+++ b/MedtrumKitUI/Views/Onboarding/PatchPrimingView.swift
@@ -3,66 +3,64 @@ import SwiftUI
 
 struct PatchPrimingView: View {
     @ObservedObject var viewModel: PatchPrimingViewModel
+    @State private var isFillGuidePresented = false
 
     var body: some View {
         VStack {
             List {
                 Section {
-                    supportImage("connect_base")
-                    HStack(alignment: .top) {
-                        Text("1.")
-                            .foregroundStyle(.primary)
-                        Text("Connect your pump base to the patch.", comment: "Label for prime step 2.1")
-                            .foregroundStyle(.primary)
-                            .frame(maxWidth: .infinity, alignment: .leading)
-                    }
-                }
-
-                Section {
-                    supportImage("fill_reservoir")
-                    VStack(alignment: .leading) {
-                        HStack(alignment: .top) {
-                            Text("2.")
-                                .foregroundStyle(.primary)
-                            Text("Fill the syringe with insulin", comment: "Label for prime step 2.2")
-                                .foregroundStyle(.primary)
-                                .frame(maxWidth: .infinity, alignment: .leading)
-                        }
-                        HStack(alignment: .top) {
-                            Text("3.")
-                                .foregroundStyle(.primary)
-                            Text(
-                                "Place the syringe in the patch and pull out 1 to 2 dashes of air.",
-                                comment: "Label for prime step 2.3"
-                            )
-                            .foregroundStyle(.primary)
-                            .frame(maxWidth: .infinity, alignment: .leading)
-                        }
-                        HStack(alignment: .top) {
-                            Text("4.")
-                                .foregroundStyle(.primary)
-                            Text(
-                                "Fill the patch with insulin. NOTE: A minimum of 70U is required for activation.",
-                                comment: "Label for prime step 2.4"
-                            )
-                            .foregroundStyle(.primary)
-                            .frame(maxWidth: .infinity, alignment: .leading)
+                    HStack {
+                        Spacer()
+                        Button {
+                            isFillGuidePresented = true
+                        } label: {
+                            Image(systemName: "info.circle")
+                                .font(.title3)
                         }
+                        .buttonStyle(.plain)
+                        .accessibilityLabel(Text("Fill guide", comment: "Accessibility label for Medtrum fill guide info button"))
                     }
-                }
 
-                Section {
-                    supportImage("half_press_needle_button")
-                    HStack(alignment: .top) {
-                        Text("5.")
-                            .foregroundStyle(.primary)
+                    VStack(spacing: 12) {
+                        ReservoirView(
+                            reservoirLevel: viewModel.reservoirLevel,
+                            fillColor: viewModel.hasMinimumFill ? .green : .yellow,
+                            maxReservoirLevel: viewModel.reservoirCapacity
+                        )
+                        .frame(height: 120)
+
                         Text(
-                            "Press the needle button and start the priming process.",
-                            comment: "Label for pressing needle button step 2.5"
+                            String(
+                                format: String(
+                                    localized: "Reservoir: %.2fU / %.0fU",
+                                    comment: "Reservoir fill level during Medtrum patch filling"
+                                ),
+                                viewModel.reservoirLevel,
+                                viewModel.reservoirCapacity
+                            )
                         )
                         .foregroundStyle(.primary)
-                        .frame(maxWidth: .infinity, alignment: .leading)
+
+                        ProgressView(progress: viewModel.reservoirProgress)
+                            .padding(.horizontal)
+
+                        if viewModel.isPreparingFill {
+                            HStack(spacing: 8) {
+                                ActivityIndicator()
+                                Text("Connecting to patch...", comment: "Status while connecting to Medtrum patch before filling")
+                                    .foregroundStyle(.secondary)
+                            }
+                        } else if viewModel.hasMinimumFill {
+                            Text("Minimum fill reached. You can start priming.", comment: "Status when Medtrum patch has enough insulin to prime")
+                                .multilineTextAlignment(.center)
+                                .foregroundStyle(.green)
+                        } else {
+                            Text("Fill at least 70U before priming.", comment: "Status when Medtrum patch needs more insulin before priming")
+                                .multilineTextAlignment(.center)
+                                .foregroundStyle(.red)
+                        }
                     }
+                    .frame(maxWidth: .infinity)
                 }
             }
             Spacer()
@@ -83,23 +81,112 @@ struct PatchPrimingView: View {
                 Text("Go back to pump base", comment: "label for go to pump base patch")
             }
             .buttonStyle(ActionButtonStyle(.secondary))
-            .disabled(viewModel.isPriming)
+            .disabled(viewModel.isPriming || viewModel.isPreparingFill)
             .padding(.horizontal)
 
-            Button(action: { viewModel.startPrime() }) {
-                if viewModel.isPriming {
-                    ActivityIndicator()
+            Button(action: {
+                if viewModel.canStartPrime {
+                    viewModel.startPrime()
                 } else {
+                    viewModel.prepareForFilling()
+                }
+            }) {
+                if viewModel.isPriming || viewModel.isPreparingFill {
+                    ActivityIndicator()
+                } else if viewModel.canStartPrime {
                     Text("Start priming", comment: "label for prime start action")
+                } else {
+                    Text("Retry connection", comment: "label for retrying Medtrum patch fill connection")
                 }
             }
-            .disabled(viewModel.isPriming)
+            .disabled(!viewModel.canStartPrime && !viewModel.canRetryFillConnection)
             .buttonStyle(ActionButtonStyle())
             .padding([.bottom, .horizontal])
         }
         .listStyle(InsetGroupedListStyle())
         .edgesIgnoringSafeArea(.bottom)
         .navigationBarBackButtonHidden(viewModel.isPriming)
+        .onAppear {
+            viewModel.prepareForFilling()
+        }
+        .sheet(isPresented: $isFillGuidePresented) {
+            NavigationView {
+                List {
+                    fillGuideSections
+                }
+                .listStyle(InsetGroupedListStyle())
+                .navigationTitle(Text("Fill guide", comment: "Title for Medtrum patch fill guide"))
+                .toolbar {
+                    ToolbarItem(placement: .confirmationAction) {
+                        Button {
+                            isFillGuidePresented = false
+                        } label: {
+                            Text("Done", comment: "Button label for closing Medtrum patch fill guide")
+                        }
+                    }
+                }
+            }
+        }
+    }
+
+    @ViewBuilder private var fillGuideSections: some View {
+        Section {
+            supportImage("connect_base")
+            HStack(alignment: .top) {
+                Text("1.")
+                    .foregroundStyle(.primary)
+                Text("Connect your pump base to the patch.", comment: "Label for prime step 2.1")
+                    .foregroundStyle(.primary)
+                    .frame(maxWidth: .infinity, alignment: .leading)
+            }
+        }
+
+        Section {
+            supportImage("fill_reservoir")
+            VStack(alignment: .leading) {
+                HStack(alignment: .top) {
+                    Text("2.")
+                        .foregroundStyle(.primary)
+                    Text("Fill the syringe with insulin", comment: "Label for prime step 2.2")
+                        .foregroundStyle(.primary)
+                        .frame(maxWidth: .infinity, alignment: .leading)
+                }
+                HStack(alignment: .top) {
+                    Text("3.")
+                        .foregroundStyle(.primary)
+                    Text(
+                        "Place the syringe in the patch and pull out 1 to 2 dashes of air.",
+                        comment: "Label for prime step 2.3"
+                    )
+                    .foregroundStyle(.primary)
+                    .frame(maxWidth: .infinity, alignment: .leading)
+                }
+                HStack(alignment: .top) {
+                    Text("4.")
+                        .foregroundStyle(.primary)
+                    Text(
+                        "Fill the patch with insulin. NOTE: A minimum of 70U is required for activation.",
+                        comment: "Label for prime step 2.4"
+                    )
+                    .foregroundStyle(.primary)
+                    .frame(maxWidth: .infinity, alignment: .leading)
+                }
+            }
+        }
+
+        Section {
+            supportImage("half_press_needle_button")
+            HStack(alignment: .top) {
+                Text("5.")
+                    .foregroundStyle(.primary)
+                Text(
+                    "Press the needle button and start the priming process.",
+                    comment: "Label for pressing needle button step 2.5"
+                )
+                .foregroundStyle(.primary)
+                .frame(maxWidth: .infinity, alignment: .leading)
+            }
+        }
     }
 
     @ViewBuilder func supportImage(_ imageName: String) -> some View {
